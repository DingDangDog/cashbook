{
  "name": "cashbook",
  "version": "0.0.12",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "docker-build": "node scripts/docker-build.ts",
    "docker-push": "node scripts/docker-push.ts",
    "docker-clean": "node scripts/docker-clean.ts",
    "tag-major": "standard-version --release-as major",
    "tag-minor": "standard-version --release-as minor",
    "tag-patch": "standard-version --release-as patch",
    "release": "node scripts/changelog.js"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
